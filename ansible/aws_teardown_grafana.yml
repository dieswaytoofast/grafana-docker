#!/usr/bin/env /usr/bin/ansible-playbook
---

- hosts: elk
  become: yes
  become_method: sudo
  vars_files:
    - "{{ elk_config_file }}"
  roles:
    - common
    - ansible_docker_utils
    - ansible_weave
    - teardown